apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cp-thunder
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-20"
spec:
  project: openchoreo
  sources:
    - repoURL: https://github.com/koorikla/openchoreo.git
      targetRevision: feat/multicluster-argocd
      ref: values
    - repoURL: ghcr.io/asgardeo/helm-charts
      chart: thunder
      targetRevision: 0.36.0
      helm:
        valueFiles:
          - $values/values/cp/thunder.yaml
  destination:
    name: in-cluster
    namespace: thunder
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
    retry:
      limit: 10
      backoff:
        duration: 10s
        factor: 2
        maxDuration: 3m
